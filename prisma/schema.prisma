generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Button {
  id    String @id @default(uuid())
  title String
  url   String?
  color String
}
